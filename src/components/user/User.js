import React from 'react';
import Navbar from '../common/Navbar';

const User = () => {
    return (
        <div>
            <div>ユーザー画面</div>
            <Navbar currentPage="user" />
        </div>
    );
}

export default User;
